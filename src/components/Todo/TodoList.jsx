import React from 'react'

const TodoList = (props) => {
  return (
    <div className='text-3xl flex justify-center items-center text-cyan-400'>
        {props.todo}</div>
  )
}

export default TodoList